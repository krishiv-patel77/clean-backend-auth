1. Implement the sqlalchemy tables first (done)

2. I need to configure alembic within the ini file and remove the sqlalchemy.url and then configure it using the config.py file and database.core file within the env.py file for alembic (done)

3. Implement authentication and run the application and test to see if everything works